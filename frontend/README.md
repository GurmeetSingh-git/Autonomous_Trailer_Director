# Director's Room

Next.js frontend for Autonomous Trailer Director.

## Run

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to the FastAPI server URL. The story map and trailer pages include a demo state so the interface is inspectable before the backend endpoints are connected.
